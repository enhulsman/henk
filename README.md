# Henk — "Homie Henk"

Personal homelab agent on the Claude Agent SDK, reached over Signal, wired to
read-only homelab surfaces. See `openspec/changes/henk-v1/` for the design and
`CLAUDE.md` for the security posture.

> Full runbook (architecture, config reference, deploy + Signal registration,
> rollback) lands with task 6.1.
